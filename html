<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My LINKTREE</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      min-height: 100vh;
      background: #0f0f0f;
      color: #ffffff;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
      background-image: 
        radial-gradient(circle at 20% 30%, rgba(100,100,255,0.06) 0%, transparent 25%),
        radial-gradient(circle at 80% 70%, rgba(200,50,150,0.05) 0%, transparent 30%);
    }
    h1 {
      font-size: 3.2rem;
      font-weight: 900;
      letter-spacing: 8px;
      text-transform: uppercase;
      margin-bottom: 60px;
      background: linear-gradient(90deg, #ffffff, #cccccc, #ffffff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 20px rgba(255,255,255,0.3);
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 18px;
      max-width: 380px;
      width: 100%;
    }
    .btn {
      aspect-ratio: 1 / 1;
      background: rgba(255, 255, 255, 0.07);
      border: 1px solid rgba(255, 255, 255, 0.12);
      border-radius: 16px;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      display: flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      color: #ffffff;
      font-size: 1.1rem;
      font-weight: 600;
      transition: all 0.25s ease;
      box-shadow: 0 8px 32px rgba(0,0,0,0.35);
      overflow: hidden;
      position: relative;
    }
    .btn:hover {
      transform: translateY(-6px) scale(1.04);
      box-shadow: 0 16px 48px rgba(0,0,0,0.5);
      border-color: rgba(255, 255, 255, 0.3);
      background: rgba(255, 255, 255, 0.12);
    }
    .btn::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: linear-gradient(135deg, transparent, rgba(255,255,255,0.08), transparent);
      opacity: 0;
      transition: opacity 0.4s;
    }
    .btn:hover::before {
      opacity: 1;
    }
    /* Optional: add icons or images later inside .btn */
  </style>
</head>
<body>

  <h1>LINKTREE</h1>

  <div class="grid">
    <!-- Replace href and text with your own links -->
    <a href="https://youtube.com/@yourchannel" class="btn">YouTube</a>
    <a href="https://instagram.com/yourusername" class="btn">Instagram</a>
    <a href="https://tiktok.com/@yourhandle" class="btn">TikTok</a>
    
    <a href="https://twitter.com/yourprofile" class="btn">Twitter / X</a>
    <a href="https://yourwebsite.com" class="btn">Website</a>
    <a href="https://discord.gg/yourserver" class="btn">Discord</a>
    
    <a href="https://open.spotify.com/artist/..." class="btn">Spotify</a>
    <a href="https://link-to-telegram" class="btn">Telegram</a>
    <a href="https://your-other-link.com" class="btn">More</a>
  </div>

</body>
</html>